<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Start Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }
        .start-container {
            background: #fff;
            padding: 60px; /* Increased padding for more space */
            box-shadow: 0 4px 15px rgba(0,0,0,0.1); /* Increased shadow for a more prominent effect */
            border-radius: 8px;
            max-width: 600px; /* Increased max-width */
            width: 100%;
        }
        h1 {
            margin-bottom: 25px; /* Increased margin for better spacing */
            color: #333;
            font-size: 2rem; /* Increased font size */
        }
        p {
            font-size: 1.4rem; /* Increased font size for better readability */
            color: #555;
            margin-bottom: 15px; /* Increased margin for better spacing */
        }
        .start-btn {
            background-color: #04AA6D;
            color: #fff;
            border: none;
            padding: 20px 40px; /* Increased padding for a larger button */
            font-size: 1.4rem; /* Increased font size for button text */
            border-radius: 8px; /* Increased border-radius for a more rounded button */
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .start-btn:hover {
            background-color: #039c58; /* Slightly darker green on hover */
        }
        .start-btn:focus {
            outline: none;
        }
    </style>
</head>
<body>
    <div class="start-container">
        <h1>Quiz Information</h1>
        <p><strong>Time Limit:</strong> <%= timeLimit %> minutes</p>
        <p><strong>Number of Questions:</strong> <%= numberOfQuestions %></p>
        <p><strong>Points to Score:</strong> <%= pointsToScore %></p>
        <p><strong>Passing Score:</strong> <%= passingScore %></p>
        <form action="/quiz" method="GET">
            <button class="start-btn" type="submit">Start Quiz</button>
        </form>
    </div>
</body>
</html>
