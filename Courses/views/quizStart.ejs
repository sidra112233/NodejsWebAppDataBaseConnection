<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= quizTitle %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: white; /* Light sky blue background */
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            position: relative; /* Position container relative for absolute positioning of the close button */
            background: linear-gradient(135deg, #f9f9f9, #e0f7fa, #f0e9d2); /* Gradient with light gray, light blue, and beige */
            padding: 40px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            max-width: 600px;
            width: 100%;
            text-align: center;
        }

        .back-button {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: #00509e; /* Light red for the close button */
            color: white;
            border: none;
            padding: 8px 12px;
            font-size: 1em;
            border-radius: 50%;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .back-button:hover {
            background-color: #004080;
        }

        h1 {
            color: #0277bd; /* Dark sky blue for the title */
            font-size: 2.5em;
            margin-bottom: 20px;
            font-weight: 700;
        }

        p {
            font-size: 1.2em;
            line-height: 1.8;
            margin-bottom: 20px;
            color: #4f5b62; /* Soft gray for text */
        }

        form {
            display: flex;
            justify-content: center;
            margin-top: 30px;
        }

        button {
            background-color: #0288d1; /* Sky blue for the button */
            color: white;
            border: none;
            padding: 12px 30px;
            font-size: 1.2em;
            border-radius: 30px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        button:hover {
            background-color: #0277bd;
            transform: translateY(-3px);
        }
    </style>
</head>

<body>
    <div class="container">
    <button class="back-button" onclick="window.history.back()">&#8592; Back</button>
        <h1><%= quizTitle %></h1>
        <p>Time Limit: <%= timeLimit %> minutes</p>
        <p>Number of Questions: <%= numberOfQuestions %></p>
        <p>Points to Score: <%= pointsToScore %></p>
        <p>Passing Score: <%= passingScore %></p>

        <!-- Button to start the quiz -->
        <form action="/quiz/start" method="post">
            <input type="hidden" name="quiz_id" value="<%= quiz_id %>">
            <button type="submit">Start Quiz</button>
        </form>
    </div>

</body>
</html>
