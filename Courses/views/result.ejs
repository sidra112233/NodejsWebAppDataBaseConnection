<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Results</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
            margin: 0;
            background: linear-gradient(135deg, #ffecd2, #fcb69f, #a1c4fd, #c2e9fb); /* More vibrant multishade light gradient background */
            color: #333;
        }
        .header {
            background-color: #ffe3e3; /* Soft pinkish multishade background color */
            padding: 15px 25px;
            text-align: left;
            border-bottom: 2px solid #ccc;
        }
        .header a {
            color: #ff9472; /* Lighter multishade color */
            text-decoration: none;
            font-size: 1.3em;
            font-weight: bold;
            transition: color 0.3s ease;
        }
        .header a:hover {
            color: #ffab73; /* Slightly darker shade */
        }
        .container {
            max-width: 600px;
            margin: 50px auto;
            border: 2px solid #ffd3b6;
            padding: 25px;
            border-radius: 12px;
            background: linear-gradient(135deg, #ffffff, #e0c3fc); /* Light gradient for the container with soft pink and lavender tones */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #ff9472; /* Lighter multishade color */
            font-size: 2.5em;
            margin-bottom: 20px;
        }
        p {
            font-size: 1.3em;
            margin-bottom: 20px;
        }
        .score {
            font-size: 2.5em;
            font-weight: bold;
            color: #b39ddb; /* Soft lavender color */
            margin-bottom: 20px;
            transition: transform 0.3s ease;
        }
        .score:hover {
            transform: scale(1.1);
        }
        h3 {
            font-size: 1.5em;
            color: #ffab73; /* Lighter shade for h3 */
            margin-bottom: 30px;
        }
        .container a {
            display: inline-block;
            padding: 12px 30px;
            background-color: #ff9472; /* Lighter multishade color */
            color: white;
            border-radius: 8px;
            text-decoration: none;
            font-size: 1.2em;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }
        .container a:hover {
            background-color: #ffab73; /* Slightly darker shade */
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Quiz Completed!</h1>
        <p>Your total score is:</p>
        <p class="score"><%= totalScore %> / 100</p>
        <h3>Your Progress: <%= progress %>%</h3>
        <a href="/dashboard">Back</a>
    </div>
</body>
</html>
