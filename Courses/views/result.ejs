<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Results</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
            margin: 0;
            background: white; 
            color: #333;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            border: 2px solid #b3e5fc;
            padding: 25px;
            border-radius: 12px;
            background: linear-gradient(135deg, #e1f5fe, #f1f8e9, #bbdefb); /* Gradient with light sky blue and soft blue */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            transform: scale(0); /* Start from small scale for pop-up animation */
            animation: popUp 0.5s ease forwards; /* Apply pop-up animation */
            position: relative;
        }

        @keyframes popUp {
            0% { transform: scale(0); }
            100% { transform: scale(1); }
        }

        h1 {
            color: #1976d2; /* Strong blue color */
            font-size: 2.5em;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.3em;
            margin-bottom: 20px;
            color: #546e7a; /* Subtle dark grey text */
        }

        .score {
            font-size: 2.5em;
            font-weight: bold;
            color: #64b5f6; /* Bright sky blue */
            margin-bottom: 20px;
            transition: transform 0.3s ease;
        }

        .score:hover {
            transform: scale(1.1);
        }

        h3 {
            font-size: 1.5em;
            color: #039be5; /* Strong medium blue color */
            margin-bottom: 30px;
        }

        .back-btn {
            display: inline-block;
            padding: 12px 30px;
            background: #00509e; /* Gradient with light blue shades */
            color: #fff;
            border-radius: 8px;
            text-decoration: none;
            font-size: 1.2em;
            transition: background-color 0.3s ease, transform 0.3s ease;
            cursor: pointer;
        }

        .back-btn:hover {
            background-color: #004080; /* Slightly darker blue */
            transform: scale(1.05);
        }

        /* Confetti Canvas */
        .confetti {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 9999;
        }
    </style>
</head>
<body>

    <canvas id="confetti" class="confetti"></canvas>

    <div class="container">
        <h1>Quiz Completed!</h1>
        <p>Your total score is:</p>
        <p class="score"><%= totalScore %> / 100</p>
        <h3>Your Progress: <%= progress %>%</h3>
        <button class="back-btn" onclick="goBack()">Back</button>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.5.1/dist/confetti.browser.min.js"></script>
    <script>
        function goBack() {
                       window.location.href = '/dashboard';

        }

        // Initialize Confetti Animation
        window.onload = function() {
            var myConfetti = confetti.create(document.getElementById('confetti'), {
                resize: true
            });

            // Confetti burst effect
            myConfetti({
                particleCount: 200,
                spread: 70,
                origin: { y: 0.6 }
            });
        }
    </script>

</body>
</html>
